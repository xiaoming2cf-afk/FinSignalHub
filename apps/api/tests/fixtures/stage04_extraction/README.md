# Stage 04 Extraction Fixtures

These fixtures are synthetic Stage 04 inputs built from Stage 03 normalized document payloads. They are used only for mock-only candidate validation.

The fixtures contain no API keys, no private documents, no live provider calls, and no paid-service dependencies. When full text is unavailable, tests must assert that a no-quote rationale is carried with lower confidence.

