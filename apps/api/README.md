# API Scaffold

This directory contains the Stage 01 FastAPI scaffold. It exposes `/health` only.

The API must not include Research Mode domain models, CRUD, database migrations, connectors, LLM adapters, evidence extraction, claim graph logic, investment advice, or product workflows in Stage 01.

