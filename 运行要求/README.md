# 运行要求

## Purpose

Stores user-provided run instructions that are inputs to FinSignalHub governance work.

## Current state

`FinSignalHub_Codex_RunLog_Autonomous_Prompt.md` is the approved user-provided RunLog-driven autonomous build prompt for Stage 00.1 and later planning.

This directory is committed as an auditable input artifact. It is not product runtime code and must not contain secrets, API keys, passwords, or private credentials.
