# Local Skills

Each skill in this directory is FinSignalHub-specific and must include YAML frontmatter plus When to use, Procedure, Required outputs, and Failure conditions.

`computer-use-gpt-pro-reviewer` is a Stage 05 governance-only recovery skill for GPT Pro page review. It must not be treated as proof that a Computer Use tool is available in the active Codex session.
